import 'package:flutter/material.dart';

import '../widgets/body_of_edite_screen.dart';

class EditeNotesScreen extends StatelessWidget {
  const EditeNotesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: BodyOfEditeScreen());
  }
}
